// check if an element is in a given array
let numbers = [1, 2, 3, 7, 8];

if (numbers.includes(7)) {
  console.log('there is a 7');
} else {
  console.log('there is not a 7');
}

// power of a given number
let base = 4;
let exponent = 3;
let result = base ** exponent;

console.log(result);
